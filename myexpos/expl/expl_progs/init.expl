int main()
{
decl
	int temp,file;
enddecl
begin
	temp=exposcall("Write",-2,"enter");
    read(file);
	temp=exposcall("Exec",file);
    if(temp==-1)then
		temp=exposcall("Write",-2,"no file");
	endif;
	return 0;
end
}

